/**
 * Shared types for forward exposure (Day 12 placeholder).
 * Engine-specific types live in @/engine/forwardExposure.
 */

/** Neutral-only projection key used in APIs. */
export type ProjectionScenarioKey = "neutral";
