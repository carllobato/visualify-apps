import { AppShellLegalFooterWithModals } from "@visualify/app-shell";

/** Single-line legal footer for the HQ signed-in shell. */
export function HqAppFooter() {
  return <AppShellLegalFooterWithModals className="hq-app-footer" />;
}
