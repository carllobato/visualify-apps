export function DefaultStreamsSeededNotice() {
  return (
    <p
      className="os-streams-seed-notice text-[length:var(--ds-text-xs)] leading-relaxed text-[var(--ds-text-secondary)]"
      role="status"
    >
      Your starting streams have been set up. You can rename or archive them anytime.
    </p>
  );
}
