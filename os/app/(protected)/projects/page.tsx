import { OsPagePlaceholder } from "@/components/OsPagePlaceholder";

export const dynamic = "force-dynamic";

export default function ProjectsPage() {
  return (
    <OsPagePlaceholder
      title="Projects"
      description="Bounded efforts with clear outcomes — not a task manager, but structured operational context."
    />
  );
}
