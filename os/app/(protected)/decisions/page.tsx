import { OsPagePlaceholder } from "@/components/OsPagePlaceholder";

export const dynamic = "force-dynamic";

export default function DecisionsPage() {
  return (
    <OsPagePlaceholder
      title="Decisions"
      description="Open choices, options, and resolution — high-signal decision memory for your operating system."
    />
  );
}
