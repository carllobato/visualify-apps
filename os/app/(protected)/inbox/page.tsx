import { OsPagePlaceholder } from "@/components/OsPagePlaceholder";

export const dynamic = "force-dynamic";

export default function InboxPage() {
  return (
    <OsPagePlaceholder
      title="Inbox"
      description="Capture and triage incoming signals. Processing and AI assistance come in a later stage."
    />
  );
}
