import { OsPagePlaceholder } from "@/components/OsPagePlaceholder";

export const dynamic = "force-dynamic";

export default function SettingsPage() {
  return (
    <OsPagePlaceholder
      title="Settings"
      description="OS preferences and configuration. Account security lives under Account in the rail menu."
    />
  );
}
